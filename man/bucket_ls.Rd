% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ls.R
\name{bucket_ls}
\alias{bucket_ls}
\title{List objects in a bucket}
\usage{
bucket_ls(
  prefix = "",
  container = NULL,
  endpoint = NULL,
  include_indexes = FALSE
)
}
\arguments{
\item{prefix}{Optional key prefix to restrict the listing, e.g.
\code{"SCANFI_v2/1985"}. Default \code{""} lists everything.}

\item{container}{The bucket (container) name. When \code{NULL} (the default),
resolved from \code{options(buckethost.container=)}, then
\code{BUCKETHOST_CONTAINER}, then a built-in default.}

\item{endpoint}{The object store's HTTPS host (no bucket name). When \code{NULL},
resolved from \code{options(buckethost.endpoint=)}, then \code{BUCKETHOST_ENDPOINT},
then a built-in default.}

\item{include_indexes}{If \code{FALSE} (default), generated \code{index.html} files
are dropped from the result so you see only data objects.}
}
\value{
A \code{data.frame} with columns \code{key}, \code{size} (bytes), and \code{modified}
(ISO-8601 string), sorted by key.
}
\description{
Reads the bucket's public S3 listing API and returns one row per object.
Pagination (1000 keys per response) is handled for you. The bucket must be
configured for public/anonymous read.
}
\examples{
\dontrun{
bucket_ls(prefix = "SCANFI_v2/1985",
          endpoint = "https://object-arbutus.cloud.computecanada.ca",
          container = "predictiveecology")
}
}
\seealso{
\code{\link[=bucket_url]{bucket_url()}}, \code{\link[=generate_indexes]{generate_indexes()}}
}
