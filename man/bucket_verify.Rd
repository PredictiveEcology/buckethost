% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verify.R
\name{bucket_verify}
\alias{bucket_verify}
\title{Verify a local tree against the bucket}
\usage{
bucket_verify(
  local_dir,
  remote_path,
  container = NULL,
  remote = NULL,
  method = c("size", "hash"),
  filter_file = NULL,
  rclone_path = "rclone"
)
}
\arguments{
\item{local_dir}{Local directory to compare.}

\item{remote_path}{Remote prefix within the bucket to compare against.}

\item{container}{The bucket (container) name. When \code{NULL} (the default),
resolved from \code{options(buckethost.container=)}, then
\code{BUCKETHOST_CONTAINER}, then a built-in default.}

\item{remote}{The name of your local \code{rclone} remote. When \code{NULL}, resolved
from \code{options(buckethost.remote=)}, then \code{BUCKETHOST_REMOTE}, then a
built-in default.}

\item{method}{\code{"size"} for a size-only check (fast) or \code{"hash"} for a
checksum comparison (thorough).}

\item{filter_file}{Optional \code{rclone} filter file, matching the one used for
the upload, so the comparison considers the same set of files.}

\item{rclone_path}{Path to the \code{rclone} executable. Default \code{"rclone"}.}
}
\value{
Invisibly, a list with \code{ok} (logical; \code{TRUE} when rclone reports no
differences) and \code{output} (rclone's captured stderr/stdout, which lists
any mismatches).
}
\description{
Wraps \verb{rclone check} to compare a local directory with a remote prefix
after an upload. \code{method = "size"} (the default, \code{--size-only}) is fast and
usually sufficient; \code{method = "hash"} compares checksums for a stronger,
slower guarantee.
}
\examples{
\dontrun{
res <- bucket_verify("~/local/SCANFI/1985", "SCANFI_v2/1985",
                     filter_file = "~/scanfi.filter")
res$ok
}
}
\seealso{
\code{\link[=bucket_upload]{bucket_upload()}}
}
