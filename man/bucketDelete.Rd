% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/upload.R
\name{bucketDelete}
\alias{bucketDelete}
\title{Delete an object or prefix from a bucket}
\usage{
bucketDelete(
  remotePath,
  container = NULL,
  remote = NULL,
  recursive = FALSE,
  confirm = TRUE,
  rclonePath = "rclone",
  dryRun = FALSE
)
}
\arguments{
\item{remotePath}{Key (single file) or prefix (with \code{recursive = TRUE}) to
delete, e.g. \code{"SCANFI_v2/1985/old.tif"}. A full public URL is accepted and
normalised to the key.}

\item{container}{The bucket (container) name. When \code{NULL} (the default),
resolved from \code{options(buckethost.container=)}, then
\code{BUCKETHOST_CONTAINER}, then a built-in default.}

\item{remote}{The name of your local \code{rclone} remote. When \code{NULL}, resolved
from \code{options(buckethost.remote=)}, then \code{BUCKETHOST_REMOTE}, then a
built-in default.}

\item{recursive}{If \code{TRUE}, delete everything under \code{remotePath} (a
prefix). If \code{FALSE} (default), delete a single object.}

\item{confirm}{If \code{TRUE} (default) and the session is interactive, prompt
before deleting.}

\item{rclonePath}{Path to the \code{rclone} executable. Default \code{"rclone"}.}

\item{dryRun}{If \code{TRUE}, pass \code{--dry-run}. Default \code{FALSE}.}
}
\value{
Invisibly, rclone's captured output, or \code{NULL} if cancelled.
}
\description{
Removes a single file (\verb{rclone deletefile}) or an entire prefix
(\verb{rclone delete}, when \code{recursive = TRUE}) from the bucket. Because this is
destructive it asks for confirmation by default; set \code{confirm = FALSE} to
delete non-interactively (e.g. in scripts).
}
\seealso{
\code{\link[=bucketUpload]{bucketUpload()}}
}
