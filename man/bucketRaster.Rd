% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/url.R
\name{bucketRaster}
\alias{bucketRaster}
\title{Read a remote raster via GDAL \verb{/vsicurl/}}
\usage{
bucketRaster(path, container = NULL, endpoint = NULL, ...)
}
\arguments{
\item{path}{Object key relative to the bucket root, e.g.
\code{"SCANFI_v2/1985/SCANFI_age_median_1985_v2.tif"}.}

\item{container}{The bucket (container) name. When \code{NULL} (the default),
resolved from \code{options(buckethost.container=)}, then
\code{BUCKETHOST_CONTAINER}, then a built-in default.}

\item{endpoint}{The object store's HTTPS host (no bucket name). When \code{NULL},
resolved from \code{options(buckethost.endpoint=)}, then \code{BUCKETHOST_ENDPOINT},
then a built-in default.}

\item{...}{Passed to \code{\link[terra:rast]{terra::rast()}}.}
}
\value{
A \link[terra:SpatRaster]{terra::SpatRaster}.
}
\description{
Convenience wrapper around \code{terra::rast("/vsicurl/<url>")}. When the object
is a Cloud-Optimized GeoTIFF, GDAL fetches only the byte ranges needed —
opening metadata or cropping to a small area transfers a few hundred KB
rather than the whole file.
}
\examples{
\dontrun{
r <- bucketRaster("SCANFI_v2/1985/SCANFI_age_median_1985_v2.tif")
r                                  # metadata only, no full download
terra::crop(r, my_aoi)             # fetches just the AOI's bytes
}
}
