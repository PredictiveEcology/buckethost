% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/upload.R
\name{bucketUpload}
\alias{bucketUpload}
\title{Upload files to a bucket}
\usage{
bucketUpload(
  local,
  remotePath,
  container = NULL,
  remote = NULL,
  filterFile = NULL,
  transfers = 16L,
  extra = character(0),
  rclonePath = "rclone",
  dryRun = FALSE,
  echo = FALSE
)
}
\arguments{
\item{local}{Path to a local file or directory to upload.}

\item{remotePath}{Destination within the bucket. Either a key/prefix like
\code{"SCANFI_v2/1985"}, or a full public URL like
\code{"https://endpoint/container/SCANFI_v2/file.csv"} — a URL is normalised to
the key (\code{"SCANFI_v2/file.csv"}) so the file lands where you expect rather
than nested under the bucket. Leading slashes are not required.}

\item{container}{The bucket (container) name. When \code{NULL} (the default),
resolved from \code{options(buckethost.container=)}, then
\code{BUCKETHOST_CONTAINER}, then a built-in default.}

\item{remote}{The name of your local \code{rclone} remote. When \code{NULL}, resolved
from \code{options(buckethost.remote=)}, then \code{BUCKETHOST_REMOTE}, then a
built-in default.}

\item{filterFile}{Optional path to an \code{rclone} filter file (\code{--filter-from})
to include/exclude by pattern. See the migration vignette for an example.}

\item{transfers}{Number of parallel transfers (\code{--transfers}). Default 16.}

\item{extra}{Character vector of additional raw \code{rclone} flags, e.g.
\code{c("--retries", "10", "--stats", "60s")}.}

\item{rclonePath}{Path to the \code{rclone} executable. Default \code{"rclone"}.}

\item{dryRun}{If \code{TRUE}, pass \code{--dry-run} so rclone reports what it \emph{would}
do without transferring. Default \code{FALSE}.}

\item{echo}{If \code{TRUE}, print the rclone command before running it.}
}
\value{
Invisibly, rclone's captured output (character).
}
\description{
Thin, loud wrapper around \code{rclone}. Copies \code{local} to \code{remotePath} within
the bucket. rclone only transfers what has changed, so re-running is cheap
and idempotent.
}
\details{
When \code{local} is a single \strong{file}, \verb{rclone copyto} is used so the file lands
at exactly \code{remotePath} (treated as the destination object key). When
\code{local} is a \strong{directory}, \verb{rclone copy} is used so its contents are synced
under \code{remotePath} (treated as a prefix). This avoids the classic \verb{rclone copy} footgun where a single file sent to a file-like destination is nested
as \code{remotePath/filename}.
}
\examples{
\dontrun{
bucketUpload("~/local/SCANFI/1985", "SCANFI_v2/1985",
              filterFile = "~/scanfi.filter",
              extra = c("--retries", "10", "--stats", "60s"))
}
}
\seealso{
\code{\link[=bucketVerify]{bucketVerify()}}, \code{\link[=bucketDelete]{bucketDelete()}}
}
