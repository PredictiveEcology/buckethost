% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mirror.R
\name{makeMirrorManifest}
\alias{makeMirrorManifest}
\title{Build a mirror manifest for a bucket}
\usage{
makeMirrorManifest(
  bucket = NULL,
  prefix = "",
  driveFolder = NULL,
  endpoint = NULL,
  file = NULL
)
}
\arguments{
\item{bucket}{Container (bucket) name. When \code{NULL} (default), resolved via
\code{\link[=bucketContainer]{bucketContainer()}}.}

\item{prefix}{Optional key prefix to restrict the manifest, e.g.
\code{"SCANFI_v2/1985"}. Default \code{""} (the whole bucket).}

\item{driveFolder}{Optional Google Drive folder id or URL. When supplied,
the folder is listed recursively and an \code{id} column is added, matched to
the bucket objects by file name. Requires the \pkg{googledrive} package.}

\item{endpoint}{Optional endpoint override (see \link{bucketConfig}).}

\item{file}{Optional path to write the manifest to as CSV. When \code{NULL}
(default) no file is written.}
}
\value{
A \code{data.frame} with columns \code{filename}, \code{key}, \code{url}, and (when
\code{driveFolder} is given) \code{id}. Returned invisibly when \code{file} is written,
visibly otherwise.
}
\description{
Produces a "remap" table: one row per object in the bucket, giving its
\code{filename}, its full object \code{key}, and its public \code{url}. Optionally adds an
\code{id} column with the matching Google Drive file id, so you can map a Drive
source to its mirrored copy on the bucket. Optionally writes the table to a
CSV.
}
\details{
Matching to Drive is by \strong{file name} (\code{basename(key)}). If the same file
name appears under more than one folder in the bucket, those rows cannot be
told apart by name alone and the \code{id} match is ambiguous; a warning is
issued listing the offending names. The \code{key} column always disambiguates
the rows themselves.
}
\examples{
\dontrun{
m <- buckethost::makeMirrorManifest(
  "predictiveecology", "SCANFI_v2/",
  driveFolder = "https://.../folders/199oEp-..."
)
write.csv(m, "arbutus_manifest_SCANFI_v2_clean.csv", row.names = FALSE)

# Or let makeMirrorManifest() write the CSV for you via `file =`:
makeMirrorManifest("predictiveecology", "SCANFI_v2/",
                   driveFolder = "https://.../folders/199oEp-...",
                   file = "arbutus_manifest_SCANFI_v2_clean.csv")
}
}
\seealso{
\code{\link[=bucketLs]{bucketLs()}}, \code{\link[=bucketUrl]{bucketUrl()}}
}
