% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/upload.R
\name{bucket_upload}
\alias{bucket_upload}
\title{Upload files to a bucket}
\usage{
bucket_upload(
  local,
  remote_path,
  container = NULL,
  remote = NULL,
  filter_file = NULL,
  transfers = 16L,
  extra = character(0),
  rclone_path = "rclone",
  dry_run = FALSE,
  echo = FALSE
)
}
\arguments{
\item{local}{Path to a local file or directory to upload.}

\item{remote_path}{Destination key/prefix within the bucket, e.g.
\code{"SCANFI_v2/1985"}. Leading slashes are not required.}

\item{container}{The bucket (container) name. When \code{NULL} (the default),
resolved from \code{options(buckethost.container=)}, then
\code{BUCKETHOST_CONTAINER}, then a built-in default.}

\item{remote}{The name of your local \code{rclone} remote. When \code{NULL}, resolved
from \code{options(buckethost.remote=)}, then \code{BUCKETHOST_REMOTE}, then a
built-in default.}

\item{filter_file}{Optional path to an \code{rclone} filter file (\code{--filter-from})
to include/exclude by pattern. See the migration vignette for an example.}

\item{transfers}{Number of parallel transfers (\code{--transfers}). Default 16.}

\item{extra}{Character vector of additional raw \code{rclone} flags, e.g.
\code{c("--retries", "10", "--stats", "60s")}.}

\item{rclone_path}{Path to the \code{rclone} executable. Default \code{"rclone"}.}

\item{dry_run}{If \code{TRUE}, pass \code{--dry-run} so rclone reports what it \emph{would}
do without transferring. Default \code{FALSE}.}

\item{echo}{If \code{TRUE}, print the rclone command before running it.}
}
\value{
Invisibly, rclone's captured output (character).
}
\description{
Thin, loud wrapper around \verb{rclone copy}. Copies \code{local} (a file or
directory) to \code{remote_path} within the bucket. rclone only transfers what
has changed, so re-running is cheap and idempotent.
}
\examples{
\dontrun{
bucket_upload("~/local/SCANFI/1985", "SCANFI_v2/1985",
              filter_file = "~/scanfi.filter",
              extra = c("--retries", "10", "--stats", "60s"))
}
}
\seealso{
\code{\link[=bucket_verify]{bucket_verify()}}, \code{\link[=bucket_delete]{bucket_delete()}}
}
