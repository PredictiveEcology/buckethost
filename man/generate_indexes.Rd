% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indexes.R
\name{generate_indexes}
\alias{generate_indexes}
\title{Generate a browsable HTML catalogue for a bucket}
\usage{
generate_indexes(
  container = NULL,
  endpoint = NULL,
  remote = NULL,
  heading = NULL,
  disclaimer_html = getOption("buckethost.disclaimer_html", ""),
  host_note = getOption("buckethost.host_note",
    "Hosted on an S3-compatible object store."),
  template = NULL,
  all_files = NULL,
  rclone_path = "rclone",
  dry_run = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{container}{The bucket (container) name. When \code{NULL} (the default),
resolved from \code{options(buckethost.container=)}, then
\code{BUCKETHOST_CONTAINER}, then a built-in default.}

\item{endpoint}{The object store's HTTPS host (no bucket name). When \code{NULL},
resolved from \code{options(buckethost.endpoint=)}, then \code{BUCKETHOST_ENDPOINT},
then a built-in default.}

\item{remote}{The name of your local \code{rclone} remote. When \code{NULL}, resolved
from \code{options(buckethost.remote=)}, then \code{BUCKETHOST_REMOTE}, then a
built-in default.}

\item{heading}{Text for the page \verb{<h1>} and \verb{<title>}. Defaults to the
container name. Set this to your repository's display name.}

\item{disclaimer_html}{Raw HTML inserted near the top of every page (the
yellow "hero" block in the default template). Use it for provenance
notes, citations, or links to authoritative sources. Default \code{""} (none).
Can also be set via \code{options(buckethost.disclaimer_html = ...)}.}

\item{host_note}{One line shown in the footer describing where the data is
hosted. Defaults via \code{options(buckethost.host_note = ...)} or a generic
string.}

\item{template}{Path to a custom HTML template. Defaults to the one shipped
with the package (see \code{system.file("templates", "index.html", package = "buckethost")}). Templates use \code{{{token}}} placeholders; see Details.}

\item{all_files}{Optional pre-fetched object table (as returned by
\code{\link[=bucket_ls]{bucket_ls()}}). Supply it to avoid re-listing the bucket, or to index a
subset. When \code{NULL} (default) the bucket is listed for you.}

\item{rclone_path}{Path to the \code{rclone} executable. Default \code{"rclone"}.}

\item{dry_run}{If \code{TRUE}, build the pages but do not upload them; the HTML
is returned (invisibly) for inspection. Default \code{FALSE}.}

\item{quiet}{Suppress progress messages. Default \code{FALSE}.}
}
\value{
Invisibly, a named character vector of the generated HTML pages
(names are directory paths, \code{""} for the root).
}
\description{
Lists every object in the bucket, then writes one \code{index.html} per
"directory" level (including the root) and uploads them back to the bucket
with \code{rclone}. The result is a static, dependency-free website: open
\verb{<base_url>/index.html} in any browser to navigate the tree, sort columns,
and click through to files. Each page also carries a configurable
disclaimer/"hero" block so context follows users wherever they land.
}
\details{
Re-run it whenever the bucket's contents change.
\subsection{Template placeholders}{

A custom \code{template} is a single HTML file in which these tokens are
substituted per page: \code{{{page_title}}}, \code{{{repo_heading}}},
\code{{{disclaimer_html}}}, \code{{{viewing_heading}}}, \code{{{breadcrumb}}},
\code{{{parent_link}}}, \code{{{rows}}}, \code{{{host_note}}}, and \code{{{timestamp}}}. The
shipped template provides sortable columns (vanilla JS, no dependencies)
with directories pinned to the top.
}
}
\examples{
\dontrun{
options(
  buckethost.endpoint  = "https://object-arbutus.cloud.computecanada.ca",
  buckethost.container = "predictiveecology",
  buckethost.remote    = "arbutus"
)
generate_indexes(
  heading = "PredictiveEcology Temporary Data Repository",
  disclaimer_html = paste0(
    "<div class='hero'><p><strong>These data are not produced by the ",
    "PredictiveEcology group</strong> and are only hosted here to ease ",
    "open data access.</p></div>"
  ),
  host_note = paste0("Hosted on the Digital Research Alliance of Canada's ",
                     "Arbutus object storage.")
)
}
}
\seealso{
\code{\link[=bucket_ls]{bucket_ls()}}, \code{\link[=bucket_upload]{bucket_upload()}}
}
