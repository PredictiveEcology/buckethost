% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/url.R
\name{bucketUrl}
\alias{bucketUrl}
\title{Build the public URL for an object}
\usage{
bucketUrl(path, container = NULL, endpoint = NULL)
}
\arguments{
\item{path}{Object key relative to the bucket root, e.g.
\code{"SCANFI_v2/1985/SCANFI_age_median_1985_v2.tif"}.}

\item{container}{The bucket (container) name. When \code{NULL} (the default),
resolved from \code{options(buckethost.container=)}, then
\code{BUCKETHOST_CONTAINER}, then a built-in default.}

\item{endpoint}{The object store's HTTPS host (no bucket name). When \code{NULL},
resolved from \code{options(buckethost.endpoint=)}, then \code{BUCKETHOST_ENDPOINT},
then a built-in default.}
}
\value{
The full HTTPS URL \code{endpoint/container/path} (character).
}
\description{
Build the public URL for an object
}
\examples{
bucketUrl("SCANFI_v2/1985/age.tif",
           endpoint = "https://object-arbutus.cloud.computecanada.ca",
           container = "predictiveecology")
}
\seealso{
\code{\link[=bucketRaster]{bucketRaster()}} for a GDAL \verb{/vsicurl/} wrapper.
}
