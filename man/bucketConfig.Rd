% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{bucketConfig}
\alias{bucketConfig}
\alias{bucketEndpoint}
\alias{bucketContainer}
\alias{bucketRemote}
\alias{bucketBaseUrl}
\alias{bucketRcloneRemote}
\title{Connection configuration}
\usage{
bucketEndpoint(endpoint = NULL)

bucketContainer(container = NULL)

bucketRemote(remote = NULL)

bucketBaseUrl(container = NULL, endpoint = NULL)

bucketRcloneRemote(container = NULL, remote = NULL)
}
\arguments{
\item{endpoint}{The object store's HTTPS host (no bucket name). When \code{NULL},
resolved from \code{options(buckethost.endpoint=)}, then \code{BUCKETHOST_ENDPOINT},
then a built-in default.}

\item{container}{The bucket (container) name. When \code{NULL} (the default),
resolved from \code{options(buckethost.container=)}, then
\code{BUCKETHOST_CONTAINER}, then a built-in default.}

\item{remote}{The name of your local \code{rclone} remote. When \code{NULL}, resolved
from \code{options(buckethost.remote=)}, then \code{BUCKETHOST_REMOTE}, then a
built-in default.}
}
\value{
A length-one character string.

\code{bucketBaseUrl()}: the public \code{endpoint/container} URL.

\code{bucketRcloneRemote()}: the \code{remote:container} string for rclone.
}
\description{
Helpers that resolve where your bucket lives. Each reads, in order, an R
option, then an environment variable, then a built-in default. Set the
options once (see \link{buckethost-package}) and the rest of the package picks
them up.
}
\details{
Two URL/identifier conventions are at play and are easy to confuse:

\describe{
\item{\code{endpoint}}{The object store's HTTPS host, e.g.
\code{"https://object-arbutus.cloud.computecanada.ca"}. No bucket name.}
\item{\code{container}}{The bucket (a.k.a. container) name, e.g.
\code{"predictiveecology"}.}
\item{\code{remote}}{The name of the \code{rclone} remote you configured with
\verb{rclone config}, e.g. \code{"arbutus"}. This is local to your machine and is
unrelated to the public URL.}
}

\code{\link[=bucketBaseUrl]{bucketBaseUrl()}} combines endpoint + container into the public base URL
for reads; \code{\link[=bucketRcloneRemote]{bucketRcloneRemote()}} combines remote + container into the
\code{remote:container} string \code{rclone} expects.
}
\examples{
bucketBaseUrl(endpoint = "https://example.com", container = "mydata")
bucketRcloneRemote(remote = "arbutus", container = "mydata")
}
